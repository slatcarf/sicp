; Find the values of the expressions
((t s) 0)
((t (t s)) 0)
(((t t) s) 0)
; where t is defined as in question 2 (5 actually, guess they made a mistake)
; above, and s is defined as follows:
(define (s x)
(+ 1 x))

; This is the same as the last exercise, since `s` is the same as `1+`